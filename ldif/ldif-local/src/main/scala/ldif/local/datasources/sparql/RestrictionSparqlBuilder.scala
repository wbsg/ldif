package ldif.local.datasources.sparql

import ldif.entity.{Restriction, Path}

/**
 * Builds a SPARQL pattern from a restriction.
 */
object RestrictionSparqlBuilder
{
  /**
   * Builds a SPARQL pattern from a restriction.
   *
   * @param restriction The restriction
   */
  def apply(restriction : Restriction) : String =
  {
    //TODO
    ""
  }
}